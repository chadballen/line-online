# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'cse335project' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for cse335project
  pod 'Firebase'
  pod 'Firebase/Database'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'

  target 'cse335projectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'cse335projectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
